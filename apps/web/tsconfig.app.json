{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": [
    "env.d.ts",
    "src/**/*",
    "src/**/*.vue"
  ],
  "exclude": [
    "src/**/__tests__/*"
  ],
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    // --- MODIFICATIONS START ---
    // "baseUrl": "." has been REMOVED.
    // The "@gen/*" alias has been REMOVED.
    // Your vite.config.ts handles the "@/*" alias,
    // so we can use that file as the source of truth.
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
    // --- MODIFICATIONS END ---
  }
}