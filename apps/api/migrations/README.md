# Migrations

This folder contains SQL files for initializing or updating the database schema.

- `001_init.sql`: Creates users and sessions tables.

To run migrations, you can use a tool like `golang-migrate`, `flyway`, or `liquibase`.