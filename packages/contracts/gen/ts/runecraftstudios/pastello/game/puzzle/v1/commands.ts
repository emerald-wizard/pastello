// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: runecraftstudios/pastello/game/puzzle/v1/commands.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GameSessionId, PlayerId } from "../../types/v1/types";

export const protobufPackage = "runecraftstudios.pastello.game.puzzle.v1";

export interface MovePieceCommand {
  sessionId?: GameSessionId | undefined;
  playerId?: PlayerId | undefined;
  fromX: number;
  fromY: number;
  toX: number;
  toY: number;
}

export interface UndoMoveCommand {
  sessionId?: GameSessionId | undefined;
  playerId?: PlayerId | undefined;
}

export interface PieceMovedEvent {
  sessionId?: GameSessionId | undefined;
  fromX: number;
  fromY: number;
  toX: number;
  toY: number;
}

export interface MoveUndoneEvent {
  sessionId?: GameSessionId | undefined;
}

function createBaseMovePieceCommand(): MovePieceCommand {
  return { sessionId: undefined, playerId: undefined, fromX: 0, fromY: 0, toX: 0, toY: 0 };
}

export const MovePieceCommand: MessageFns<MovePieceCommand> = {
  encode(message: MovePieceCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined) {
      GameSessionId.encode(message.sessionId, writer.uint32(10).fork()).join();
    }
    if (message.playerId !== undefined) {
      PlayerId.encode(message.playerId, writer.uint32(18).fork()).join();
    }
    if (message.fromX !== 0) {
      writer.uint32(24).int32(message.fromX);
    }
    if (message.fromY !== 0) {
      writer.uint32(32).int32(message.fromY);
    }
    if (message.toX !== 0) {
      writer.uint32(40).int32(message.toX);
    }
    if (message.toY !== 0) {
      writer.uint32(48).int32(message.toY);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MovePieceCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMovePieceCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = GameSessionId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerId = PlayerId.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromX = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fromY = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toX = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.toY = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MovePieceCommand {
    return {
      sessionId: isSet(object.sessionId) ? GameSessionId.fromJSON(object.sessionId) : undefined,
      playerId: isSet(object.playerId) ? PlayerId.fromJSON(object.playerId) : undefined,
      fromX: isSet(object.fromX) ? globalThis.Number(object.fromX) : 0,
      fromY: isSet(object.fromY) ? globalThis.Number(object.fromY) : 0,
      toX: isSet(object.toX) ? globalThis.Number(object.toX) : 0,
      toY: isSet(object.toY) ? globalThis.Number(object.toY) : 0,
    };
  },

  toJSON(message: MovePieceCommand): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined) {
      obj.sessionId = GameSessionId.toJSON(message.sessionId);
    }
    if (message.playerId !== undefined) {
      obj.playerId = PlayerId.toJSON(message.playerId);
    }
    if (message.fromX !== 0) {
      obj.fromX = Math.round(message.fromX);
    }
    if (message.fromY !== 0) {
      obj.fromY = Math.round(message.fromY);
    }
    if (message.toX !== 0) {
      obj.toX = Math.round(message.toX);
    }
    if (message.toY !== 0) {
      obj.toY = Math.round(message.toY);
    }
    return obj;
  },

  create(base?: DeepPartial<MovePieceCommand>): MovePieceCommand {
    return MovePieceCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MovePieceCommand>): MovePieceCommand {
    const message = createBaseMovePieceCommand();
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null)
      ? GameSessionId.fromPartial(object.sessionId)
      : undefined;
    message.playerId = (object.playerId !== undefined && object.playerId !== null)
      ? PlayerId.fromPartial(object.playerId)
      : undefined;
    message.fromX = object.fromX ?? 0;
    message.fromY = object.fromY ?? 0;
    message.toX = object.toX ?? 0;
    message.toY = object.toY ?? 0;
    return message;
  },
};

function createBaseUndoMoveCommand(): UndoMoveCommand {
  return { sessionId: undefined, playerId: undefined };
}

export const UndoMoveCommand: MessageFns<UndoMoveCommand> = {
  encode(message: UndoMoveCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined) {
      GameSessionId.encode(message.sessionId, writer.uint32(10).fork()).join();
    }
    if (message.playerId !== undefined) {
      PlayerId.encode(message.playerId, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UndoMoveCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUndoMoveCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = GameSessionId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerId = PlayerId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UndoMoveCommand {
    return {
      sessionId: isSet(object.sessionId) ? GameSessionId.fromJSON(object.sessionId) : undefined,
      playerId: isSet(object.playerId) ? PlayerId.fromJSON(object.playerId) : undefined,
    };
  },

  toJSON(message: UndoMoveCommand): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined) {
      obj.sessionId = GameSessionId.toJSON(message.sessionId);
    }
    if (message.playerId !== undefined) {
      obj.playerId = PlayerId.toJSON(message.playerId);
    }
    return obj;
  },

  create(base?: DeepPartial<UndoMoveCommand>): UndoMoveCommand {
    return UndoMoveCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UndoMoveCommand>): UndoMoveCommand {
    const message = createBaseUndoMoveCommand();
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null)
      ? GameSessionId.fromPartial(object.sessionId)
      : undefined;
    message.playerId = (object.playerId !== undefined && object.playerId !== null)
      ? PlayerId.fromPartial(object.playerId)
      : undefined;
    return message;
  },
};

function createBasePieceMovedEvent(): PieceMovedEvent {
  return { sessionId: undefined, fromX: 0, fromY: 0, toX: 0, toY: 0 };
}

export const PieceMovedEvent: MessageFns<PieceMovedEvent> = {
  encode(message: PieceMovedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined) {
      GameSessionId.encode(message.sessionId, writer.uint32(10).fork()).join();
    }
    if (message.fromX !== 0) {
      writer.uint32(16).int32(message.fromX);
    }
    if (message.fromY !== 0) {
      writer.uint32(24).int32(message.fromY);
    }
    if (message.toX !== 0) {
      writer.uint32(32).int32(message.toX);
    }
    if (message.toY !== 0) {
      writer.uint32(40).int32(message.toY);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PieceMovedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePieceMovedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = GameSessionId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fromX = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromY = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.toX = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toY = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PieceMovedEvent {
    return {
      sessionId: isSet(object.sessionId) ? GameSessionId.fromJSON(object.sessionId) : undefined,
      fromX: isSet(object.fromX) ? globalThis.Number(object.fromX) : 0,
      fromY: isSet(object.fromY) ? globalThis.Number(object.fromY) : 0,
      toX: isSet(object.toX) ? globalThis.Number(object.toX) : 0,
      toY: isSet(object.toY) ? globalThis.Number(object.toY) : 0,
    };
  },

  toJSON(message: PieceMovedEvent): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined) {
      obj.sessionId = GameSessionId.toJSON(message.sessionId);
    }
    if (message.fromX !== 0) {
      obj.fromX = Math.round(message.fromX);
    }
    if (message.fromY !== 0) {
      obj.fromY = Math.round(message.fromY);
    }
    if (message.toX !== 0) {
      obj.toX = Math.round(message.toX);
    }
    if (message.toY !== 0) {
      obj.toY = Math.round(message.toY);
    }
    return obj;
  },

  create(base?: DeepPartial<PieceMovedEvent>): PieceMovedEvent {
    return PieceMovedEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PieceMovedEvent>): PieceMovedEvent {
    const message = createBasePieceMovedEvent();
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null)
      ? GameSessionId.fromPartial(object.sessionId)
      : undefined;
    message.fromX = object.fromX ?? 0;
    message.fromY = object.fromY ?? 0;
    message.toX = object.toX ?? 0;
    message.toY = object.toY ?? 0;
    return message;
  },
};

function createBaseMoveUndoneEvent(): MoveUndoneEvent {
  return { sessionId: undefined };
}

export const MoveUndoneEvent: MessageFns<MoveUndoneEvent> = {
  encode(message: MoveUndoneEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined) {
      GameSessionId.encode(message.sessionId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MoveUndoneEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMoveUndoneEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = GameSessionId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MoveUndoneEvent {
    return { sessionId: isSet(object.sessionId) ? GameSessionId.fromJSON(object.sessionId) : undefined };
  },

  toJSON(message: MoveUndoneEvent): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined) {
      obj.sessionId = GameSessionId.toJSON(message.sessionId);
    }
    return obj;
  },

  create(base?: DeepPartial<MoveUndoneEvent>): MoveUndoneEvent {
    return MoveUndoneEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MoveUndoneEvent>): MoveUndoneEvent {
    const message = createBaseMoveUndoneEvent();
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null)
      ? GameSessionId.fromPartial(object.sessionId)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
