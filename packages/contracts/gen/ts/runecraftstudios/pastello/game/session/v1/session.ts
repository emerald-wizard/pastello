// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: runecraftstudios/pastello/game/session/v1/session.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { PuzzleRules } from "../../puzzle/v1/rules";
import { TriviaRules } from "../../trivia/v1/rules";
import {
  GameSessionId,
  GameType,
  gameTypeFromJSON,
  gameTypeToJSON,
  PlayerId,
  SessionStatus,
  sessionStatusFromJSON,
  sessionStatusToJSON,
} from "../../types/v1/types";

export const protobufPackage = "runecraftstudios.pastello.game.session.v1";

export interface GameSession {
  id?: GameSessionId | undefined;
  gameType: GameType;
  playerIds: PlayerId[];
  status: SessionStatus;
  createdAt?:
    | Date
    | undefined;
  /** optional external reference */
  rulesetId: string;
}

export interface CreateGameSessionRequest {
  gameType: GameType;
  playerIds: PlayerId[];
  config?: { $case: "trivia"; trivia: TriviaRules } | { $case: "puzzle"; puzzle: PuzzleRules } | {
    $case: "rulesetId";
    rulesetId: string;
  } | undefined;
}

export interface CreateGameSessionResponse {
  session?: GameSession | undefined;
}

function createBaseGameSession(): GameSession {
  return { id: undefined, gameType: 0, playerIds: [], status: 0, createdAt: undefined, rulesetId: "" };
}

export const GameSession: MessageFns<GameSession> = {
  encode(message: GameSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      GameSessionId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.gameType !== 0) {
      writer.uint32(16).int32(message.gameType);
    }
    for (const v of message.playerIds) {
      PlayerId.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.rulesetId !== "") {
      writer.uint32(50).string(message.rulesetId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = GameSessionId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gameType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.playerIds.push(PlayerId.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.rulesetId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameSession {
    return {
      id: isSet(object.id) ? GameSessionId.fromJSON(object.id) : undefined,
      gameType: isSet(object.gameType) ? gameTypeFromJSON(object.gameType) : 0,
      playerIds: globalThis.Array.isArray(object?.playerIds)
        ? object.playerIds.map((e: any) => PlayerId.fromJSON(e))
        : [],
      status: isSet(object.status) ? sessionStatusFromJSON(object.status) : 0,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      rulesetId: isSet(object.rulesetId) ? globalThis.String(object.rulesetId) : "",
    };
  },

  toJSON(message: GameSession): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = GameSessionId.toJSON(message.id);
    }
    if (message.gameType !== 0) {
      obj.gameType = gameTypeToJSON(message.gameType);
    }
    if (message.playerIds?.length) {
      obj.playerIds = message.playerIds.map((e) => PlayerId.toJSON(e));
    }
    if (message.status !== 0) {
      obj.status = sessionStatusToJSON(message.status);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.rulesetId !== "") {
      obj.rulesetId = message.rulesetId;
    }
    return obj;
  },

  create(base?: DeepPartial<GameSession>): GameSession {
    return GameSession.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GameSession>): GameSession {
    const message = createBaseGameSession();
    message.id = (object.id !== undefined && object.id !== null) ? GameSessionId.fromPartial(object.id) : undefined;
    message.gameType = object.gameType ?? 0;
    message.playerIds = object.playerIds?.map((e) => PlayerId.fromPartial(e)) || [];
    message.status = object.status ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.rulesetId = object.rulesetId ?? "";
    return message;
  },
};

function createBaseCreateGameSessionRequest(): CreateGameSessionRequest {
  return { gameType: 0, playerIds: [], config: undefined };
}

export const CreateGameSessionRequest: MessageFns<CreateGameSessionRequest> = {
  encode(message: CreateGameSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameType !== 0) {
      writer.uint32(8).int32(message.gameType);
    }
    for (const v of message.playerIds) {
      PlayerId.encode(v!, writer.uint32(18).fork()).join();
    }
    switch (message.config?.$case) {
      case "trivia":
        TriviaRules.encode(message.config.trivia, writer.uint32(82).fork()).join();
        break;
      case "puzzle":
        PuzzleRules.encode(message.config.puzzle, writer.uint32(90).fork()).join();
        break;
      case "rulesetId":
        writer.uint32(98).string(message.config.rulesetId);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateGameSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateGameSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.gameType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerIds.push(PlayerId.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.config = { $case: "trivia", trivia: TriviaRules.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.config = { $case: "puzzle", puzzle: PuzzleRules.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.config = { $case: "rulesetId", rulesetId: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateGameSessionRequest {
    return {
      gameType: isSet(object.gameType) ? gameTypeFromJSON(object.gameType) : 0,
      playerIds: globalThis.Array.isArray(object?.playerIds)
        ? object.playerIds.map((e: any) => PlayerId.fromJSON(e))
        : [],
      config: isSet(object.trivia)
        ? { $case: "trivia", trivia: TriviaRules.fromJSON(object.trivia) }
        : isSet(object.puzzle)
        ? { $case: "puzzle", puzzle: PuzzleRules.fromJSON(object.puzzle) }
        : isSet(object.rulesetId)
        ? { $case: "rulesetId", rulesetId: globalThis.String(object.rulesetId) }
        : undefined,
    };
  },

  toJSON(message: CreateGameSessionRequest): unknown {
    const obj: any = {};
    if (message.gameType !== 0) {
      obj.gameType = gameTypeToJSON(message.gameType);
    }
    if (message.playerIds?.length) {
      obj.playerIds = message.playerIds.map((e) => PlayerId.toJSON(e));
    }
    if (message.config?.$case === "trivia") {
      obj.trivia = TriviaRules.toJSON(message.config.trivia);
    } else if (message.config?.$case === "puzzle") {
      obj.puzzle = PuzzleRules.toJSON(message.config.puzzle);
    } else if (message.config?.$case === "rulesetId") {
      obj.rulesetId = message.config.rulesetId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateGameSessionRequest>): CreateGameSessionRequest {
    return CreateGameSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateGameSessionRequest>): CreateGameSessionRequest {
    const message = createBaseCreateGameSessionRequest();
    message.gameType = object.gameType ?? 0;
    message.playerIds = object.playerIds?.map((e) => PlayerId.fromPartial(e)) || [];
    switch (object.config?.$case) {
      case "trivia": {
        if (object.config?.trivia !== undefined && object.config?.trivia !== null) {
          message.config = { $case: "trivia", trivia: TriviaRules.fromPartial(object.config.trivia) };
        }
        break;
      }
      case "puzzle": {
        if (object.config?.puzzle !== undefined && object.config?.puzzle !== null) {
          message.config = { $case: "puzzle", puzzle: PuzzleRules.fromPartial(object.config.puzzle) };
        }
        break;
      }
      case "rulesetId": {
        if (object.config?.rulesetId !== undefined && object.config?.rulesetId !== null) {
          message.config = { $case: "rulesetId", rulesetId: object.config.rulesetId };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseCreateGameSessionResponse(): CreateGameSessionResponse {
  return { session: undefined };
}

export const CreateGameSessionResponse: MessageFns<CreateGameSessionResponse> = {
  encode(message: CreateGameSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      GameSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateGameSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateGameSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = GameSession.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateGameSessionResponse {
    return { session: isSet(object.session) ? GameSession.fromJSON(object.session) : undefined };
  },

  toJSON(message: CreateGameSessionResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = GameSession.toJSON(message.session);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateGameSessionResponse>): CreateGameSessionResponse {
    return CreateGameSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateGameSessionResponse>): CreateGameSessionResponse {
    const message = createBaseCreateGameSessionResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? GameSession.fromPartial(object.session)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
